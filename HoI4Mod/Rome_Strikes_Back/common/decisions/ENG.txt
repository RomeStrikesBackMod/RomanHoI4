england_decisions = {
	eng_invite_nearby_communists = {
		visible = {
			has_completed_focus = ENG_red_torch
			FROM = {
				exists = yes
				NOT = { is_subject_of = ENG }
				NOT = { is_in_faction_with = ENG }
			}
		}
		targets = { HAI BIR UTA COL ARG }
		available = {
			FROM = {
				has_government = communism
				is_in_faction = no
			}
		}
		complete_effect = {
			FROM = {
				country_event = tesb_england.20
			}
		}
	}
}